module tb_mux4to1;
    // ?????????? ?????? ??? ??????????? ?? ?????? ????????
    reg [7:0] D0, D1, D2, D3;
    reg [1:0] A;
    wire [7:0] Q;

    // ??????????? ??? ????????????? (uut - unit under test)
    mux4to1 uut (
        .D0(D0), .D1(D1), .D2(D2), .D3(D3),
        .A(A), .Q(Q)
    );

    // ????, ???? ??????????? ????? ???????
    initial begin
        // ???????? ?????????? ? ??????? (?? ? ????????)
        $monitor("Time = %0t | A = %b | Q = %b", $time, A, Q);

        // ??????? ???????????? ?????
        D0 = 8'b11110000;
        D1 = 8'b00001111;
        D2 = 8'b10101010;
        D3 = 8'b01010101;

        // ??????????? ?????? ? ????????? ? 10 ??????? ????
        A = 2'b00; #10;
        A = 2'b01; #10;
        A = 2'b10; #10;
        A = 2'b11; #10;

        // ??????????? ?????????? ?????????
        $finish;
    end
endmodule
